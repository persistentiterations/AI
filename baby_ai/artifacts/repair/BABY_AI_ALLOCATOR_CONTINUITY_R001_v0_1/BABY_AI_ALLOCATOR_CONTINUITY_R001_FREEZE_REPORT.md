# BABY AI ALLOCATOR CONTINUITY R001 — FREEZE REPORT

**Tranche:** R-001 allocator continuity (HOST ONLY — Motorola untouched).
**Repair premise:** on the deployed handset, formation survived but allocator
identity did not — a reset-on-load id allocator re-issued inherited identifiers.

## Prior authority

- Implementation freeze: `9d6317351f74032c77883db198d637777af4a33b` (CROSS_CONTEXT_RESOLVE).
- Frozen package: `BABY_AI_FORMATIONCORE_CROSS_CONTEXT_RESOLVE_v0_1`.
- Base lineage: R8 `0e64df6` → R9 `797598a` → MARK/RESOLVE `d64c45b` → CROSS_CONTEXT `9d63173`.

## New R-001 commit

Recorded by annotated tag `BABY_AI_ALLOCATOR_CONTINUITY_R001_v0_1`; resolve via
`git rev-list -1 BABY_AI_ALLOCATOR_CONTINUITY_R001_v0_1`. This report and the
freeze manifest are committed together with the code; the tag is the authority
pointer.

## New tag

`BABY_AI_ALLOCATOR_CONTINUITY_R001_v0_1` (annotated).

## Files changed (R-001 only)

- `baby_ai/adapters/operational_self.py` — `id_continuation` block, `reconcile_allocator`,
  `_derive_counters`, `_block_allocator`, `formation_ready`, `allocator_continuation`,
  `allocator_family_ids`, `apply_operator_allocator_override`, `formation_blocked` gating.
- `baby_ai/tests/test_formation.py` — 4 added tests (3 continuation + 1 reset-on-load witness).
- `baby_ai/core/migration_receipts.py` — `MigrationReceiptLedger` (permanent operator handoffs).
- `baby_ai/qualifications/r001_closure.py` — qualification harness (gates G1–G8).
- `baby_ai/assays/allocator_continuity_freeze.py` — deterministic freeze generator.
- `baby_ai/artifacts/repair/BABY_AI_ALLOCATOR_CONTINUITY_R001_v0_1/` — freeze package
  (`FREEZE_MANIFEST.json`, `WITNESS_ALLOCATOR_CONTINUITY.json`, `TEST_RECEIPT.json`,
  `MIGRATION_POLICY.json`, `DIRTY_TREE_PREFLIGHT.json`, this report).

## Governed invariant

After serialized state is reloaded, newly allocated identifiers continue from
authoritative persisted allocator state and cannot collide with identifiers
already present in retained state. Families: `mem, attr, fog, scar, lnk, replay, evt`.

## Tests

`python -m pytest baby_ai/tests -q` → **100 passed**.

## Device-shaped regression witness

`test_allocator_continuity_reset_on_load_regression` (and
`WITNESS_ALLOCATOR_CONTINUITY.json`) reproduce the observed failure class:

- **Observed on device (evidence, not fixture):** 90,073 memories; 100,395 fog
  objects; 10,322 duplicate fog-id suffixes; deployed bytecode pre-R-001.
- **pre-R-001 reload (fixture):** memory-id collision = true; fog duplicate suffix = 1.
- **R-001 reload (fixture):** `formation_ready`; `allocator_status.kind = persisted`;
  next memory id `mem-0006` continues beyond retained max (5); collision = false;
  fog duplicate = 0.

## Previous-freeze integrity

- R8 / R9 / MARK/RESOLVE / CROSS_CONTEXT packages byte-identical (git status clean).
- Ladder `representations.py` NOT modified by R-001 (diff touches only `operational_self.py` + `test_formation.py`).
- Ladder E: 55/55 `all_correct`, cause fidelity 1.0 (regenerated byte-identical).
- CROSS_CONTEXT regeneration byte-identical at HEAD; MARK/RESOLVE byte-identical at its own commit.

## Unresolved residuals (unchanged, not addressed here)

- `GLOBAL_MARK_SCOPED_QUERY_READPATH` (logged orthogonal).
- repeated-MARK + one-RESOLVE collision.
- SUPERSEDE + RESOLVE surface divergence.

## Dirty/scratch artifacts intentionally excluded

`APPLICABILITY_REPAIR_v0_1` + `baby_ai/assays/applicability.py` + `repair_freeze.py`
(separate tranche), and ~57 scratch files (`bolt_*`, `fx_*`, `v12_*`, `scan_*`, …).
Preserved on disk, not committed. Full inventory in `DIRTY_TREE_PREFLIGHT.json`.

## Recommended M2 starting point (not started here)

Reusable components for the persistent HOLD/PROCEED cycle:
`FormationCore.from_dict` (reconcile + restore), `ContinuitySnapshot` (pack/read),
`ReceiptLedger`/`ProvenanceLedger` (ledgers), `route_decision` (HOLD/PROCEED surface),
`MigrationReceiptLedger` (operator handoff). A standalone runner that persists →
restarts → re-routes with a printed authority trace is the only new shell needed.

---

**R-001 STATUS:** FROZEN
**AUTHORITY SHA:** `9d6317351f74032c77883db198d637777af4a33b` (implementation) — new R-001 commit via tag
**TAG:** `BABY_AI_ALLOCATOR_CONTINUITY_R001_v0_1`
**TESTS:** 100 passed
**MOTOROLA TOUCHED:** NO
**PRIOR FREEZES CHANGED:** NONE
**NEXT ACTION:** build the persistent HOLD/PROCEED cycle runner on host (M2)
**DO NOT DO YET:** do not rebuild the APK, do not touch handset state, do not resume device formation
